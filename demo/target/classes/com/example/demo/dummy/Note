@RequestParam(value = "id"): GET /resorts ? id = num <- 무조건 id 파라미터가 있어야 함.
@PathVariable(value = "id"): GET /resorts/{id} 

ResponseEntity는 Body에 class를 실어 보낼때 사용.

	/* 1. 이 메서드가 직접 /test로 값을 return해준다.
	@ExceptionHandler(NullPointerException.class)
	public String returnStatus() {
		System.out.println("operated");
		return "False";
	}
	*/